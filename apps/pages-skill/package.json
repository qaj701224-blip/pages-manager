{
  "name": "@xd-cell/skill",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "files": ["dist/xd-cell", "BREAKING_CHANGES.md"],
  "scripts": {
    "build": "node src/build.js",
    "test": "node --test src/*.test.js",
    "prepack": "pnpm run build"
  }
}
