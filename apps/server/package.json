{
  "name": "@xd/server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @xd-cell/worker-sdk build && wrangler dev",
    "deploy": "pnpm --filter @xd-cell/worker-sdk build && wrangler deploy"
  },
  "dependencies": {
    "@xd/ip-guard": "workspace:*",
    "@xd/pages-runtime-protocol": "workspace:*",
    "@xd-cell/worker-sdk": "workspace:*",
    "@xd/worker-kit": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "catalog:"
  }
}
