<!-- This file is generated by apps/worker-sdk/scripts/generate-llms.js. Do not edit by hand. -->

# @xd-cell/worker-sdk API

## 生成来源

- 包入口：`package.json` 的 `exports["."]`
- TypeScript 声明：`dist/worker/index.d.ts`

## 公开 API 摘要

```ts
export declare function createRuntime(options: {
    env: RuntimeEnv;
    request?: Request;
}): Runtime;

export declare function readContext(request: Request): RuntimeContext | null;

export declare class SDKError extends Error {
    readonly code: string;
    readonly status?: number;
    readonly details?: unknown;
    constructor(code: string, message: string, status?: number, details?: unknown);
}

export interface KVGetOptions {
    type?: KVValueType;
}

export interface KVNamespace {
    get(key: string, options?: {
        type?: 'text';
    }): Promise<string | null>;
    get<T = unknown>(key: string, options: {
        type: 'json';
    }): Promise<T | null>;
    put(key: string, value: string, options?: {
        type?: 'text';
        expirationTtl?: number;
    }): Promise<void>;
    put(key: string, value: unknown, options: {
        type: 'json';
        expirationTtl?: number;
    }): Promise<void>;
    delete(key: string): Promise<void>;
}

export interface KVPutOptions {
    type?: KVValueType;
    expirationTtl?: number;
}

export type KVValueType = 'json' | 'text';

export interface Runtime {
    kv: KVNamespace;
}

export interface RuntimeContext {
    authenticated: boolean;
    anonymous: boolean;
    userId: string | null;
    siteId: string;
    siteUuid: string;
    siteSlug: string;
    routeId: string;
    versionId: string;
    policyVersion: number;
    traceId: string;
    environment: string;
}

export interface RuntimeEnv {
    [binding: string]: unknown;
}
```

## 约束

- 本文只描述 `@xd-cell/worker-sdk` 根导出的公开 API。
- 为了让公开类型可读，本文会展开 public export 引用到的支持类型声明。
- 不包含 internal modules、browser helper、adapter 或 inline runtime source。
- API 变化必须先更新源码和类型声明，再重新生成本文。
