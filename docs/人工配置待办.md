# XD Pages v2 人工配置待办

> 临时执行清单，用完可以删除。不要在这里填写真实 secret、token、account id、zone id、KV id、D1 id 或 SSO secret。

## 0. 操作原则

- [ ] v1 `*.workers.xd.team`、`api.workers.xd.team`、旧 Worker、旧 route、旧 workflow 全部不动。
- [ ] v2 只新增 `*.pages.xd.team`、`api.pages.xd.team`、`auth.pages.xd.team` 相关资源。
- [ ] staging / production 资源物理隔离，不复用 D1、KV、WFP dispatch namespace、普通 Worker slot 池、JWT secret、access key pepper。
- [ ] production workflow 只能手动触发，不能被 push / PR 自动触发。
- [ ] `docs/xd-sso.md` 只允许作为本地临时参考，不提交；上线前确认已删除。
- [ ] 所有真实 secret、token、Cloudflare 资源 id、SSO client secret 只放 Cloudflare 控制台、GitHub Environment vars/secrets 或本地 ignored env。

## 1. Cloudflare 前置验证

- [ ] 确认当前账号是否已开通 Workers for Platforms；如暂未开通，首发保持 v2 wrangler template 中 `PAGES_EXECUTION_MODE=normal-worker-slot`。
- [ ] 若启用 WFP，确认 dispatch namespace、user Worker 数量、CPU、子请求、日志、费用和告警配额。
- [ ] 若启用普通 Worker slot，确认 service binding 数量上限、普通 Worker 数量、部署耗时、日志、费用和告警配额。
- [ ] 验证 `pages.xd.team` / `*.pages.xd.team` 的 DNS、CNAME、证书 DCV 可行。
- [ ] v2 system Worker 的 route / custom domain 由 `wrangler.*.template.toml` 声明，首次部署会在 Cloudflare 建立绑定；partial zone 下 DNSPod CNAME、DCV 委派和证书状态仍需人工确认。
- [ ] 验证 route 优先级符合预期：
  - `api.pages.xd.team/*` 命中 `pages-api`
  - `auth.pages.xd.team/*` 命中 `pages-auth`
  - `api-staging.pages.xd.team/*` 命中 `pages-api-staging`
  - `auth-staging.pages.xd.team/*` 命中 `pages-auth-staging`
  - `*-staging.pages.xd.team/*` 优先命中 `pages-router-staging`
  - `*.pages.xd.team/*` 命中 `pages-router`
- [ ] 如果 `*-staging.pages.xd.team/*` 无法稳定优先于 wildcard route，改用无业务 secret 的 `pages-edge-router-thin` fallback。
- [ ] 确认 `pages-edge-router-thin` 如启用，只做 hostname 分流和 service binding 转发，不持有 D1/KV/DO、dispatch namespace、slot binding、JWT secret、CF token 或 SSO secret。

## 2. Cloudflare 资源创建

### Production

- [ ] Worker：`pages-api`
- [ ] Worker：`pages-auth`
- [ ] Worker：`pages-router`
- [ ] Worker：`pages-kv-gateway`，现有空预留资源直接划归 v2。
- [ ] WFP dispatch namespace：`pages-production`。如首发使用 `normal-worker-slot`，可先记录为待开通。
- [ ] 普通 Worker slot 池：`pages-v2-production-slot-001..N`，首发按小规模试点容量创建。
- [ ] `pages-router` service binding：`SITE_SLOT_001..SITE_SLOT_N` -> 对应 production slot Worker。
- [ ] D1 database：`pages_metadata_production`
- [ ] KV namespace：`pages_router_cache_production`
- [ ] KV namespace：`pages-shared-data`，现有 key count = 0 的空 namespace 直接划归 v2 site data。
- [ ] Durable Object namespaces：OAuth state、CLI login、auth session、route lock / policy coordination 相关 binding。
- [ ] Route / custom domain 由 v2 wrangler template 随部署声明创建，部署后确认：
  - `api.pages.xd.team` custom domain -> `pages-api`
  - `auth.pages.xd.team` custom domain -> `pages-auth`
  - `*.pages.xd.team/*` route -> `pages-router`
- [ ] 记录 D1 database id、KV namespace id、account id 到 GitHub `production` Environment vars，不写入仓库。

### Staging

- [ ] Worker：`pages-api-staging`
- [ ] Worker：`pages-auth-staging`
- [ ] Worker：`pages-router-staging`
- [ ] Worker：`pages-kv-gateway-staging`，现有空预留资源直接划归 v2。
- [ ] WFP dispatch namespace：`pages-staging`。如首发使用 `normal-worker-slot`，可先记录为待开通。
- [ ] 普通 Worker slot 池：`pages-v2-staging-slot-001..N`，首发按小规模试点容量创建。
- [ ] `pages-router-staging` service binding：`SITE_SLOT_001..SITE_SLOT_N` -> 对应 staging slot Worker。
- [ ] D1 database：`pages_metadata_staging`
- [ ] KV namespace：`pages_router_cache_staging`
- [ ] KV namespace：`pages-shared-data-staging`，现有 key count = 0 的空 namespace 直接划归 v2 site data。
- [ ] Durable Object namespaces：staging 独立 binding。
- [ ] Route / custom domain 由 v2 wrangler template 随部署声明创建，部署后确认：
  - `api-staging.pages.xd.team` custom domain -> `pages-api-staging`
  - `auth-staging.pages.xd.team` custom domain -> `pages-auth-staging`
  - `*-staging.pages.xd.team/*` route -> `pages-router-staging`
- [ ] 记录 D1 database id、KV namespace id、account id 到 GitHub `staging` Environment vars，不写入仓库。

### v1 关闭项（代码侧已完成，部署后确认）

- [x] v1 `apps/server` 不再接受 `kv=true`，请求直接返回 `KV_NOT_SUPPORTED`。
- [x] v1 deploy workflow 不再部署 `apps/kv-gateway`，也不再向 `apps/server` 注入 `PAGES_CAP_JWT_*`。
- [x] v1 README / API / skill 不再提供 Pages KV SDK 用法。
- [x] v2 部署流水线覆盖 `pages-api`、`pages-auth`、`pages-router`、`pages-kv-gateway` 四个系统 Worker；`pages-kv-gateway` 使用 v2 wrangler template、v2 workflow 和 `scripts/put-pages-v2-secrets.sh` 注入 capability signing secret。

### 普通 Worker slot 池扩容流程

- [x] `scripts/provision-pages-v2-slots.mjs` 已纳入 v2 deploy workflow；slot 扩容在 router 部署前自动执行，不需要 GitHub var 总数。
- [ ] 扩容脚本按当前环境历史最大 `slot_number` 递增创建缺失编号，不覆盖已存在 Worker，不修改已 `assigned` slot。
- [ ] ordinary Worker 创建成功且 `workers.dev` subdomain 已关闭后，写入 D1 `worker_slots(status=available_pending_router)`。
- [ ] 抽查 `pages-v2-*-slot-*` 的 `workers.dev` 直连地址不可访问；关闭失败的 slot 必须保持不可分配。
- [ ] 部署脚本输出 `PAGES_NORMAL_WORKER_SLOT_BINDING_COUNT=max(worker_slots.slot_number)`，router wrangler 渲染全量绑定 `SITE_SLOT_001..SITE_SLOT_N`，不能丢失老 slot binding。
- [ ] 部署对应环境 router 并注入 router secrets 成功后，把被当前 binding count 覆盖的 `available_pending_router` 标记为 `available`。
- [ ] smoke 失败或 router 部署失败时保留 `available_pending_router` 或标记 `disabled`，不得让 `pages-api` 分配。
- [ ] 配置容量告警：`available` slot 低于阈值、`available_pending_router` 长时间存在、`disabled` 增长、slot deploy 失败率异常；首发 `pages-api` 在 slot 耗尽导致 deploy 返回 `DEPLOYMENT_CAPACITY_EXHAUSTED` 时通过 Slack webhook 发告警。

## 3. Cloudflare Token 与权限

- [ ] 创建 GitHub Actions 部署用 token，保存为 `CLOUDFLARE_API_TOKEN`，仅用于 Wrangler 部署；权限需覆盖 Worker 部署、Worker route 和 custom domain 声明创建/更新。
- [ ] 创建 `pages-api` runtime 调 Cloudflare Workers / WFP API 的 token，保存为 `CF_API_TOKEN`，只注入 `pages-api`。
- [ ] 确认 `CF_API_TOKEN` 不注入 `pages-auth`、`pages-router`、user Worker、CLI、`.pages.json` 或公开文档。
- [ ] 当前 router route 在 wrangler template 中使用 `zone_name = "xd.team"`，不需要把 zone id 写入仓库；如后续改为按 API 管理 DNS/DCV，再单独评估 zone 级 secret。

## 4. 心动 SSO 应用申请

### Production

- [ ] 应用名称：`xd_pages`
- [ ] 描述：`XD Pages 内部站点发布服务`
- [ ] SSO 认证方式：OAuth
- [ ] 用户访问入口：`https://auth.pages.xd.team/.xd-pages/auth/authorize`
- [ ] SSO 认证重定向地址：`https://auth.pages.xd.team/.xd-pages/auth/callback`
- [ ] 账号映射属性：邮箱
- [ ] 授权用户范围：心动

### Staging

- [ ] 应用名称：`xd_pages_staging`
- [ ] 描述：`XD Pages 内部站点发布服务测试环境`
- [ ] SSO 认证方式：OAuth
- [ ] 用户访问入口：`https://auth-staging.pages.xd.team/.xd-pages/auth/authorize`
- [ ] SSO 认证重定向地址：`https://auth-staging.pages.xd.team/.xd-pages/auth/callback`
- [ ] 账号映射属性：邮箱
- [ ] 授权用户范围：心动

### Local

- [ ] 应用名称：`xd_pages_local`
- [ ] 描述：`XD Pages 内部站点发布服务本地开发环境`
- [ ] SSO 认证方式：OAuth
- [ ] 用户访问入口：`http://xd-pages.127.0.0.1.nip.io:8787/.xd-pages/auth/authorize`
- [ ] SSO 认证重定向地址：`http://xd-pages.127.0.0.1.nip.io:8787/.xd-pages/auth/callback`
- [ ] 账号映射属性：邮箱
- [ ] 授权用户范围：心动

### SSO 需要确认

- [ ] profile 是否提供稳定 user id。
- [ ] profile 是否提供邮箱。
- [ ] profile 是否提供员工状态，并给出到 `active`、`disabled`、`left`、`unknown` 的映射表。
- [ ] 如果第一版要支持部门 ACL，profile 或组织接口需要提供稳定 department id / path。
- [ ] token endpoint 是否仍按心动 SSO 文档使用 `GET /cas/oauth2.0/accessToken?...`，profile 是否仍使用 `GET /cas/oauth2.0/profile?access_token=...`。
- [ ] 除心动 SSO token/profile 接口要求的服务端 query 参数外，确认 `client_secret`、OAuth code、access token 不会出现在浏览器 URL、Referer、错误响应或日志；平台不得记录完整 token/profile 请求 URL。
- [ ] 确认日志、审计和用户可见错误会脱敏 `client_secret`、OAuth code、access token、`st`、`tgtId` 等 SSO 票据字段。
- [ ] 确认 production / staging 的 `SSO_CLIENT_ID` 已按当前 template 固定为 `xd_pages` / `xd_pages_staging`；`SSO_CLIENT_SECRET` 必须放 secret。

## 5. GitHub Environments

- [ ] 创建 GitHub Environment：`staging`
- [ ] 创建 GitHub Environment：`production`
- [ ] production Environment 开启人工审批或至少限制触发权限。
- [ ] staging 和 production 分别配置独立 vars/secrets。

### 可随 Git 提交的配置

下表为非敏感、可审查、首发不需要环境人员频繁修改的配置，已写入 v2 `wrangler.*.template.toml` 或 v2 deploy workflow。修改这些值应走普通 PR，而不是手动改 GitHub Environment。

| 名称 | 当前值 | 使用方 | 说明 |
| ---- | ------ | ------ | ---- |
| `PAGES_ENV` | production / staging template 固定 | 全部 v2 Worker | 环境边界常量。 |
| `PAGES_EXECUTION_MODE` | `normal-worker-slot` | `pages-api`、`pages-router` | 平台内部执行模式；首发因 WFP 未开通固定为普通 Worker slot。WFP 验证完成后通过 PR 把对应 template 改为 `wfp`。 |
| `PUBLIC_API_BASE` | `https://api.pages.xd.team` / `https://api-staging.pages.xd.team` | `pages-api`、`pages-auth`、`pages-router` | 公开 API 地址。 |
| `PUBLIC_AUTH_BASE` | `https://auth.pages.xd.team` / `https://auth-staging.pages.xd.team` | `pages-api`、`pages-auth`、`pages-router` | SSO 入口地址。 |
| `PUBLIC_SITE_SUFFIX` | `pages.xd.team` | `pages-api`、`pages-auth`、`pages-router` | v2 子站域名后缀。 |
| `SLACK_PAGES_ALERT_MENTION_USER_ID` | `U06QLFY2XCK` | `pages-api` | slot 容量告警中要 @ 的 Slack 成员 id；只用于消息 mention，不是 webhook secret。 |
| `WFP_DISPATCH_NAMESPACE` | `pages-production` / `pages-staging` | `pages-api`、`pages-router` | WFP namespace 名称；`normal-worker-slot` 首发时可暂不实际启用。 |
| `WFP_COMPATIBILITY_DATE` | `2026-06-15` | `pages-api` | 部署用户 Worker 到 WFP 时使用的 compatibility date。 |
| `SSO_AUTHORIZATION_URL` | `https://sso.security.xindong.com/cas/oauth2.0/authorize` | `pages-auth` | 心动 SSO OAuth authorize endpoint；非 secret，随 template 审查。 |
| `SSO_TOKEN_URL` | `https://sso.security.xindong.com/cas/oauth2.0/accessToken` | `pages-auth` | 心动 SSO token endpoint；非 secret，随 template 审查。 |
| `SSO_PROFILE_URL` | `https://sso.security.xindong.com/cas/oauth2.0/profile` | `pages-auth` | 心动 SSO profile endpoint；非 secret，随 template 审查。 |
| `SSO_CLIENT_ID` | `xd_pages` / `xd_pages_staging` | `pages-auth` | 心动 SSO OAuth client id；production / staging 分别固定。 |
| `SSO_REDIRECT_URI` | auth host 下 `/.xd-pages/auth/callback` | `pages-auth` | 必须与公司 SSO 应用申请一致。 |
| `SSO_ALLOWED_USER_SCOPE` | `xindong` | `pages-auth` | 允许登录的用户范围标识。 |
| `OAUTH_STATE_TTL_SECONDS` | `300` | `pages-auth` | OAuth state 有效期。 |
| `CLI_LOGIN_TTL_SECONDS` | `600` | `pages-auth` | CLI 浏览器登录轮询任务有效期。 |
| `AUTH_SESSION_IDLE_TTL_SECONDS` | `1209600` | `pages-auth` | `auth_session` 闲置过期时间。 |
| `AUTH_SESSION_ABSOLUTE_TTL_SECONDS` | `2592000` | `pages-auth` | `auth_session` 绝对过期时间。 |
| `SITE_SESSION_IDLE_TTL_SECONDS` | `604800` | `pages-auth`、`pages-router` | `site_session` 闲置过期时间。 |
| `SITE_SESSION_ABSOLUTE_TTL_SECONDS` | `2592000` | `pages-auth` | `site_session` 绝对过期时间。 |
| `SITE_SESSION_FRESHNESS_TTL_SECONDS` | `900` | `pages-router` | 受保护子站身份 freshness 窗口。 |
| `ROUTE_CACHE_TTL_SECONDS` | `10` | `pages-router` | router L1 route snapshot 缓存时间。 |
| `INTERNAL_WORKER_JWT_TTL_SECONDS` | `60` | `pages-router` | router 发给 User Worker 的内部 JWT 有效期。 |
| `ACCESS_KEY_ACTIVE_PEPPER_ID` | `pepper_2026_06` | `pages-api`、v2 deploy workflow | 当前新签发 access key 使用的 pepper id。 |
| `ACCESS_KEY_PEPPERS` | `pepper_2026_06:ACCESS_KEY_PEPPER_202606` | `pages-api`、v2 deploy workflow | 只写 secret 名，不写真实 pepper。 |
| `PAGES_SESSION_JWT_ACTIVE_KID` | `pages-session-2026-06` | `pages-auth`、`pages-router`、v2 deploy workflow | 当前 session/internal JWT key id。 |
| `PAGES_SESSION_JWT_KEYS` | `pages-session-2026-06:HS256:PAGES_SESSION_JWT_SECRET_202606` | `pages-auth`、`pages-router`、v2 deploy workflow | 只写 secret 名，不写真实 signing secret。 |
| `PAGES_CAP_JWT_ACTIVE_KID` | `pages-cap-2026-06` | `pages-router`、`pages-kv-gateway`、v2 deploy workflow | 当前 Pages KV capability key id。 |
| `PAGES_CAP_JWT_KEYS` | `pages-cap-2026-06:HS256:PAGES_CAP_JWT_SECRET_202606` | `pages-router`、`pages-kv-gateway`、v2 deploy workflow | 只写 secret 名，不写真实 signing secret。 |
| `PAGES_NORMAL_WORKER_SLOT_MIN_AVAILABLE` | `1` | `pages-router` template、v2 deploy workflow | slot 可用容量低于该值时触发部署期扩容。 |
| `PAGES_NORMAL_WORKER_SLOT_EXPAND_BY` | `2` | `pages-api` 告警、`pages-router` template、v2 deploy workflow | 单次部署期扩容创建的 slot 数；`pages-api` 只用于 Slack 告警展示。 |
| `PAGES_NORMAL_WORKER_SLOT_MAX_TOTAL` | `100` | `pages-router` template、v2 deploy workflow | 当前环境普通 Worker slot 总上限，超过时 fail closed。 |
| `PAGES_NORMAL_WORKER_SLOT_BINDING_COUNT` | provision 脚本计算 | `pages-router` wrangler 渲染 | 不手工配置；由 `scripts/provision-pages-v2-slots.mjs` 写入 `$GITHUB_ENV`。 |
| `SITE_SLOT_001..SITE_SLOT_N` | renderer 生成 | `pages-router` | 由部署脚本计算出的 binding count 全量生成，扩容后需要重新渲染并部署 router。 |
| `PAGES_SESSION_JWT_ISSUER` | `pages-router` | `pages-router` | 当前 router template 固定。 |
| `ROUTER_JWKS_URL` | auth host 下 `/.xd-pages/jwks.json` | `pages-router` | 当前按环境固定到对应 auth host。 |
| v2 route / custom domain | `api*.pages.xd.team`、`auth*.pages.xd.team`、子站 wildcard route | `pages-api`、`pages-auth`、`pages-router` | 写入 wrangler template，随部署创建或更新 Cloudflare 绑定；DNSPod CNAME / DCV 仍需人工确认。 |

### Vars

Vars 放非敏感、不可随 public Git 提交、但允许有配置权限人员可见的环境配置。当前 workflow 会读取下表中的 GitHub Environment vars：

| 完成 | 名称 | 必填 | 使用方 | 说明 |
| ---- | ---- | ---- | ------ | ---- |
| [ ] | `CLOUDFLARE_ACCOUNT_ID` | 是 | Wrangler、`pages-api` runtime | Cloudflare account id；workflow 用于渲染 `account_id`，并作为 `CF_ACCOUNT_ID` 注入 `pages-api`。 |
| [ ] | `PAGES_V2_D1_DATABASE_ID` | 是 | `pages-api` wrangler 渲染 | 当前环境 D1 metadata database id；staging 和 production 必须不同。 |
| [ ] | `PAGES_V2_ROUTE_SNAPSHOTS_KV_ID` | 是 | `pages-api`、`pages-router` wrangler 渲染 | 当前环境 route snapshot KV namespace id；staging 和 production 必须不同。 |
| [ ] | `PAGES_V2_SITE_DATA_KV_ID` | 是 | `pages-kv-gateway` wrangler 渲染 | 当前环境 Pages KV site data namespace id；staging 和 production 必须不同。 |
| [ ] | `ROUTER_IP_ALLOWLIST_CIDRS` | 是 | `pages-router` | 公司内网、VPN、办公出口和必要代理出口 CIDR；第一版强制配置，缺失或格式错误必须 fail closed。 |

### Secrets

Secrets 只放 token、client secret、签名密钥、pepper 等不可见高敏配置。不要写入文档、代码、wrangler template、CLI config 或 `.pages.json`。

| 完成 | 名称 | 必填 | 使用方 | 说明 |
| ---- | ---- | ---- | ------ | ---- |
| [ ] | `CLOUDFLARE_API_TOKEN` | 是 | GitHub Actions / Wrangler | 部署系统 Worker 的 token；只给 workflow/wrangler 用，不能作为 Worker runtime secret 注入。 |
| [ ] | `CF_API_TOKEN` | 是 | `pages-api` runtime | `pages-api` 调 Cloudflare Workers / WFP API 用的 token；只注入 `pages-api`，不能注入 auth/router/user Worker。 |
| [ ] | `SLACK_PAGES_ALERT_WEBHOOK_URL` | 是 | `pages-api` runtime | Slack Incoming Webhook URL；用于 slot 容量耗尽等运维告警。必须放 GitHub Environment secret，不放 Vars、wrangler template、文档或日志。 |
| [ ] | `SSO_CLIENT_SECRET` | 是 | `pages-auth` runtime | 心动 SSO OAuth client secret；只注入 `pages-auth`，不能给 CLI、router 或 user Worker。 |
| [ ] | `ACCESS_KEY_PEPPER_*` | 是 | `pages-api` runtime | access key HMAC 真实 pepper；必须覆盖 `ACCESS_KEY_PEPPERS` 中引用的每个 secret 名。 |
| [ ] | `PAGES_SESSION_JWT_SECRET_*` | 是 | `pages-auth`、`pages-router` runtime | session/internal JWT 真实 signing secret；必须覆盖 `PAGES_SESSION_JWT_KEYS` 中引用的每个 secret 名。 |
| [ ] | `PAGES_CAP_JWT_SECRET_*` | 是 | `pages-router`、`pages-kv-gateway` runtime | capability JWT 真实 signing secret；必须覆盖 `PAGES_CAP_JWT_KEYS` 中引用的每个 secret 名。 |

当前 template/workflow 固定引用 `ACCESS_KEY_PEPPER_202606`、`PAGES_SESSION_JWT_SECRET_202606`、`PAGES_CAP_JWT_SECRET_202606`；workflow 和 secret 注入脚本固定引用 `SLACK_PAGES_ALERT_WEBHOOK_URL`。轮换 key 时需要同一个 PR 更新 template registry、workflow job env / secret env 映射，并在对应 Environment 增加新 secret。Slack webhook 泄漏或测试时公开展示过，需要在 Slack 后台重新生成并替换 GitHub Environment secret。

### 建议值检查

- [ ] production `PAGES_ENV=production`
- [ ] production `PUBLIC_API_BASE=https://api.pages.xd.team`
- [ ] production `PUBLIC_AUTH_BASE=https://auth.pages.xd.team`
- [ ] production `PUBLIC_SITE_SUFFIX=pages.xd.team`
- [ ] production wrangler template 已声明 `api.pages.xd.team`、`auth.pages.xd.team` custom domain 和 `*.pages.xd.team/*` router route。
- [ ] production wrangler template 当前 `PAGES_EXECUTION_MODE=normal-worker-slot`（WFP 暂未开通首发）；切到 `wfp` 必须走 PR 修改 template。
- [ ] production 若从 `normal-worker-slot` 切到 `wfp`，先确认是否仍有 active / rollback window 内 slot route；存在时继续由 provision 脚本输出 binding count，让 router 同时持有 WFP dispatch namespace 和 slot bindings。
- [ ] production 已先执行 `apps/pages-api/migrations/` D1 migration，再部署 `pages-api` Worker。
- [ ] production `WFP_DISPATCH_NAMESPACE=pages-production`（仅 WFP 模式实际启用；仍需保持 template 环境值正确）。
- [ ] staging `PAGES_ENV=staging`
- [ ] staging `PUBLIC_API_BASE=https://api-staging.pages.xd.team`
- [ ] staging `PUBLIC_AUTH_BASE=https://auth-staging.pages.xd.team`
- [ ] staging `PUBLIC_SITE_SUFFIX=pages.xd.team`
- [ ] staging wrangler template 已声明 `api-staging.pages.xd.team`、`auth-staging.pages.xd.team` custom domain 和 `*-staging.pages.xd.team/*` router route。
- [ ] staging wrangler template 当前 `PAGES_EXECUTION_MODE=normal-worker-slot`（WFP 暂未开通首发）；切到 `wfp` 必须走 PR 修改 template。
- [ ] staging 若从 `normal-worker-slot` 切到 `wfp`，先确认是否仍有 active / rollback window 内 slot route；存在时继续由 provision 脚本输出 binding count，让 router 同时持有 WFP dispatch namespace 和 slot bindings。
- [ ] staging 已先执行 `apps/pages-api/migrations/` D1 migration，再部署 `pages-api-staging` Worker。
- [ ] staging `WFP_DISPATCH_NAMESPACE=pages-staging`（仅 WFP 模式实际启用；仍需保持 template 环境值正确）。
- [ ] `ACCESS_KEY_PEPPERS` template 里只写 `pepperId:ACCESS_KEY_PEPPER_*`，不写真实 pepper。
- [ ] `PAGES_SESSION_JWT_KEYS` template 里只写 `kid:HS256:PAGES_SESSION_JWT_SECRET_*`，不写真实 signing secret。
- [ ] `PAGES_CAP_JWT_KEYS` template 里只写 `kid:HS256:PAGES_CAP_JWT_SECRET_*`，不写真实 signing secret。
- [ ] `ROUTER_IP_ALLOWLIST_CIDRS` 必填；缺失或格式错误时 router 应 fail closed。
- [ ] `normal-worker-slot` 模式下，D1 `worker_slots` 只允许分配 `available` 状态；`available_pending_router` 不能被分配给站点。

## 6. 首次 Staging 验证

- [ ] 手动触发 `Deploy Pages V2 Staging`，`component=all`。
- [ ] 确认 wrangler template 渲染成功。
- [ ] 确认四个 `DRY_RUN=1 scripts/put-pages-v2-secrets.sh ...` 步骤先通过。
- [ ] 确认 `pages-api-staging`、`pages-auth-staging`、`pages-kv-gateway-staging`、`pages-router-staging` 均部署成功。
- [ ] 确认 `api-staging.pages.xd.team`、`auth-staging.pages.xd.team` custom domain 和 `*-staging.pages.xd.team/*` route 已由部署创建/更新。
- [ ] 确认 DNSPod CNAME、Cloudflare custom hostname / 证书 DCV 已生效；首次 custom domain 生效可能晚于 Worker 部署。
- [ ] 确认真实 secret 注入步骤成功，日志不出现 secret 明文。
- [ ] 访问 `https://api-staging.pages.xd.team/openapi.json`，只出现 staging API/auth/domain，不出现 production 或 v1 `workers.xd.team` API 地址。
- [ ] 访问 `https://api-staging.pages.xd.team/skill.md` 和 `https://api-staging.pages.xd.team/readme.md`，确认下发内容指向 v2 staging。
- [ ] 执行 `pages login --env staging`，完成 SSO 和 CLI token 保存。
- [ ] 执行 `pages deploy ./dist --env staging --slug <slug> --visibility org`。
- [ ] 若 `PAGES_EXECUTION_MODE=normal-worker-slot`，确认新版本分配到一个 `assigned` slot，route snapshot 激活后切到新 binding；后续重复 deploy 不覆盖当前 active slot，旧 slot 保留到回滚窗口后再清理。
- [ ] 若 `PAGES_EXECUTION_MODE=wfp`，确认 user Worker 落到 staging dispatch namespace。
- [ ] 执行 `pages status --env staging`。
- [ ] 执行 `pages open --env staging --print`。
- [ ] 验证 staging 子站 IP allowlist：公司网络内可继续鉴权链路，公司网络外直接 403。
- [ ] 验证 `public`、`org`、`acl`、`owner`、`disabled` visibility。
- [ ] 验证浏览器伪造 `CF-Platform-*` / `X-Pages-*` header 会被清洗。
- [ ] 验证 User Worker 设置父域 cookie 或平台保留 cookie 会被拒绝或删除。
- [ ] 验证 rollback 不覆盖旧 active version。
- [ ] 验证 v1 `api.workers.xd.team` 和 `*.workers.xd.team` 不受影响。

## 7. Production 发布前检查

- [ ] staging smoke checklist 全部通过。
- [ ] Cloudflare route / DNS / certificate 只新增 `pages.xd.team` 资源，未修改 v1 `workers.xd.team`。
- [ ] production GitHub Environment 使用独立 D1、KV、执行面资源、SSO app、JWT secret、access key pepper、IP allowlist。
- [ ] 若 `PAGES_EXECUTION_MODE=normal-worker-slot`，production slot 池和 router service binding 已 ready；若为 `wfp`，`pages-production` dispatch namespace 已 ready。
- [ ] `Deploy Pages V2 Production` 只支持 `workflow_dispatch`。
- [ ] 首次 production 发布使用 `component=all`。
- [ ] 发布后先验证 `https://api.pages.xd.team/openapi.json`。
- [ ] 验证 `https://auth.pages.xd.team/.xd-pages/auth/authorize` 登录入口。
- [ ] 发布一个受控试点站点，验证 `org` visibility、SSO、site session、header/cookie 清洗和审计。
- [ ] 回滚只通过重新 dispatch 上一个已知好 commit 或按组件部署上一个 commit，不修改 v1 route。

## 8. 本地和仓库检查

- [ ] `git status --short --untracked-files=all`
- [ ] `git diff --check`
- [ ] `git ls-files --error-unmatch docs/xd-sso.md` 期望失败，表示本地 SSO 参考未被跟踪。
- [ ] `node --test scripts/render-pages-v2-wrangler.test.js scripts/pages-v2-secrets.test.js scripts/workflows.test.js`
- [ ] `pnpm lint`
- [ ] `pnpm test`

## 9. 待确认 Owner

- [ ] Cloudflare route 优先级和 certificate/DCV spike owner。
- [ ] 公司内网、VPN、办公出口 CIDR 的权威来源、更新频率和紧急回滚 owner。
- [ ] WFP 配额、billing、告警 owner。
- [ ] 普通 Worker slot 池容量、扩容 workflow、binding 上限和告警 owner。
- [ ] SSO profile 字段映射和禁用/离职状态 freshness SLA owner。
- [ ] 审计日志保留周期、查询方式、脱敏规则 owner。
- [ ] `pages-kv-gateway` / `pages-shared-data*` v2 首发 owner。
